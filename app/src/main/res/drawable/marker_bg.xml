<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/white" />
    <corners android:radius="8dp" />
    <!-- Borde blanco -->
    <stroke
        android:width="2dp"
        android:color="@color/naranja" /> <!-- blanco -->
</shape>